import { DisplaystatusPipe } from './displaystatus.pipe';

describe('DisplaystatusPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplaystatusPipe();
    expect(pipe).toBeTruthy();
  });
});
